# Country flag assets

The offline PNG flag set is sourced from the public `stefangabos/world_countries`
project (flat 32×24 family, itself based on `lipis/flag-icons`). The European Union
flag is supplied separately as `eu.png`. Assets are used locally by NORA VPN so
flag rendering does not depend on a network request or an emoji font.
